# 当前状态与待办

_更新：2026-09-10（你去洗澡期间我继续做的：备份加固 + 跑步计时器）_

## 这轮新增

### 1. 备份加固三点（已 push、已上线）
- 未备份提醒条：累计 6 次未备份或距上次 14 天，首页弹「立即备份」
- 「保存备份」按钮走 iOS 分享面板（存备忘录 / 文件 / 发给自己）
- 备份格式加 `version` + `exportedAt`，导入兼容旧格式
- `⚙ 备份` 里有「上次备份：X」状态行

### 2. 跑步计时器（本地已提交，见下方 push）
- 「接下来」卡片上新增 **▶ 开始计时**
- **走跑结合**（月 1 的 12 次）：自动循环 跑 4′→走 1′…，大倒计时 + 「第 2/4 组」+ 总剩时间 + 进度条
- **单段连续跑**（24 次「轻松跑/马拉松心率跑 X 分钟」）：单段倒计时
- **其余 27 次**（节奏跑/间歇/按距离/比赛日）：不自动循环，给一个秒表 + 计划文字
- **提示音**代替震动（iOS 网页不支持震动）：进跑段三声上行、进走段两声下行、段末 3 秒读秒、结束三长音
- **屏幕常亮**（Wake Lock）：开始时申请，暂停/结束释放；回前台自动重申请
- **节拍音**开关：165–190 步/分（默认 180，正好对上教练的步频要求），也顺带帮页面在后台不被挂起
- 结束后可「顺手打卡」
- 页面里写明限制：不支持震动用提示音；锁屏放兜里提示音可能延后

TDD：`src/segments.js`（解析文字→段落）+ `src/timer.js`（时间轴 / stateAt / frameCues 提示音判断 / 时钟格式）+ `src/audio.js`（Web Audio，薄壳）。测试 **78 个用例全绿**。

## 线上地址（已经能用）

## 线上地址（已经能用）

**<https://lihaoran1993912.github.io/run-half-marathon/app/>**

iPhone Safari 打开 → 分享 → 添加到主屏幕。第一次连 WiFi 打开一次（缓存整包），之后断网也能用。

## 代码状态

你在的时候钥匙串是解锁的，我把「备份加固」也顺手 push 了，已上线。
「跑步计时器」这几个提交我也会 push（如果 push 失败就是钥匙串又锁了，你回来 `git push` 一下即可）：

```
cd ~/github/lihaoran1993912/myprojects/run-half-marathon
git status      # 看有没有还没 push 的
git push
```

## 已完成

- **v0.3 主体**：20 周 / 63 次训练（你已核对），按顺序打卡的指针模型，
  进度条 / 「接下来」大卡片 / 六格统计 / 全部计划 / 备份导入导出
- **打卡历史**：新增「最近完成」卡片；「全部计划」里每次训练显示完成日期、每周显示教练的话
- **加固**：完成按钮点一下即禁用（防手抖连点多打卡）；日期输入加无障碍标签；
  文本统一转义；iOS 状态栏样式改成 `default`（避免浅色下状态栏文字看不见）
- 测试 42 个用例全绿（`cd app && npm test`）
- `preview.html` 单文件兜底版已同步重建

## 回来后在手机上过一遍

1. 打卡一次 → 看「最近完成」、进度条、全部计划里的日期
2. 「保存备份」→ 走分享面板存一份到备忘录；看「上次备份」状态行更新
3. **计时器（重点，我没法在这台机器上真机验）**：
   - 第 1 次训练点「▶ 开始计时」→ 点「开始」→ 看倒计时、听跑/走切换提示音、段末读秒
   - 锁屏等 10 秒再解锁 → 倒计时应立刻跳到正确位置（按墙上时间算的）
   - 开「节拍音」听有没有「哒哒」，拖 BPM 有没有变快慢
   - 试一个「轻松跑 30 分钟」的（单段倒计时）和一个节奏跑的（应显示秒表）
   - 结束后点「顺手打卡」
4. 关 WiFi 和蜂窝，从主屏幕图标打开，确认能离线用

## 以后可做（不急）

- 计时器：真机若发现锁屏后提示音延迟太多，考虑加「静音保活音轨」或只做屏幕常亮场景
- 节奏跑/间歇跑：如果你想要，可以让计时器也支持（默认组间休息 90 秒，可调）
- 改错日期：`store.setDate()` 逻辑和测试已有，还没接界面
- 「全部计划」按月折叠；深色模式单独配 `theme-color`
