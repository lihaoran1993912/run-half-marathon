# 当前状态与待办

_更新：2026-09-15（第三次真实跑步反馈：提示音被打断后没自己恢复，加了「手动恢复」）_

## 2026-09-15：加了「提示音卡住了就手动恢复」

你反馈：第三次跑步，节拍器和跑走提示音又被耳机"当前锻炼圆环已合上"的提示音
打断了——说明下面「2026-09-11：修了『戴表+耳机跑步时提示音被打断』」那次改的
自动恢复，这次没能自愈。

**工程判断**：上次修的是"打断时不要硬排到过去的时刻，等 `resume()` 真的
`resolve` 了再排"——这个逻辑本身没错，但它有一个前提：`resume()` 最终得
`resolve`。iOS 上，*没有用户手势*触发的 `resume()`（也就是代码自己定时
轮询去调的那种）在某些系统打断类型下可能永远等不到系统把音频还回来——这不
是网页这层能验证或绕开的系统行为，只能靠"卡住太久就让用户自己点一下"来
兜底（真实的点击是货真价实的用户手势，权限更高，更可能救回来）。

**改了什么**（`src/audio.js` + `app.js`，95→97 测试全绿）：
- `beeper.isHealthy()`：`ctx` 不存在或 `running` 算健康，否则不健康
- `beeper.manualResume()`：不等旧 `ctx` 的 `resume()`，直接整个拆了重建——
  新建的 `AudioContext` 只要是在这次点击的手势里创建的，iOS 上会直接是
  `running`（这条规则跟 `unlock()` 依赖的是同一条）
- 计时器界面：每帧探一下 `isHealthy()`，卡住满 3 秒（避开打断刚开始的正
  常瞬间）就在节拍音开关下面弹出一条「提示音好像被打断后没恢复」+
  「恢复提示音」按钮，点一下就用真实手势救回音频、把节拍器（如果开着）
  重新排上
- 版本号 `v6→v7`（`src/version.js` + `sw.js` CACHE 一起改了）

**这次也没法真机验**（开发机没浏览器/iPhone）。回来后麻烦：
1. 正常跑一次，看这次是不是就不再被打断了（如果自动恢复这次生效，你可能
   根本看不到那条「恢复提示音」的提示——这是正常的，说明没触发条件）
2. 如果又被打断且提示音卡住超过 3 秒：应该会看到「恢复提示音」按钮，点一
   下应该立刻响一声确认音；顺手告诉我节拍音有没有跟着接回来
3. 如果这次「恢复提示音」按钮出现了但点了也没用（听不到确认音）：这说明
   连"真实点击"都救不回来，是我猜错了系统行为，需要换思路（比如提示你把
   耳机摘一下/重新戴一下，那是 JS 完全管不到的系统级蓝牙路由问题）

## 2026-09-11 又一轮：底部加版本号 + 修好了国内兜底版

你问"能不能加个版本号方便确认是不是最新版"——加了，页面最底下一行小灰字
现在是 `v6 · 2026-09-11` 这样。以后我发布新版会同步更新这个数字，你联网
刷新后一看底部数字对不对就知道是不是拿到了最新代码。

**顺手排查时发现一个已经存在好几周的真 bug**：`app/preview.html`（国内
打不开 GitHub Pages 时的单文件兜底方案）从跑步计时器功能上线那次起就
**打不开**——脚本化验证过：一执行就是 `ReferenceError`，因为打包脚本
没跟上新增的四个模块。这次一并修好、重新生成了。如果你之前试过
preview.html 打不开，不是你的问题，是这个 bug；现在应该好了，但我这边
没浏览器，麻烦你有空的时候点一下 `preview.html` 确认能正常显示。

## 2026-09-11：修了「戴表+耳机跑步时提示音被打断」

你反馈的现象：跑步时用 Apple Watch + 耳机，手表自己的语音提示（圆环合上、
每公里报配速）响起时，会打断 App 的步频器和走跑切换音。

**查到的真实代码 bug**（不是瞎猜手表行为，是代码里能实锤的漏洞）：
- 之前修节拍器失声那次（`7c967e6`），只改了 `scheduleMetro()`：打断时不
  硬排、等真的 running 了再排。但走跑切换音 `cue()` 和段末读秒 `tick()`
  当时**没有跟着改**——`cue()` 只认 `state==='suspended'`，漏了 iOS 打断
  系统音时专用的 `'interrupted'`；`tick()` 干脆没有任何 resume 逻辑。
  一旦切换/读秒恰好撞上手表说话的那几秒，这一声就**永久丢了**，不会补。
- 用假 AudioContext 写了 `test/audio.test.js` 复现：5 个用例里 4 个在旧
  代码上是红的。改完全绿（93 个用例）。已 push、线上已更新。

**这次没法真机验**（这台开发机没有浏览器/iPhone）。回来后麻烦跑一次：
1. 计时器开着、戴表戴耳机，故意等一次手表的语音提示（比如报配速那一下）
   响起时，看看走跑切换音 / 段末读秒有没有正常响（尤其是提示音结束后的
   那一下，是「立刻响」还是「补响」还是「没了」）
2. 如果还是有漏音，麻烦告诉我大概是「哪种提示音」「手表提示响起后过了几
   秒」——这能帮我判断是不是还有没堵上的口子（比如手表和耳机之间做了一次
   完整的蓝牙切换，不只是"让一让"）
3. 顺手：这次也把 `sw.js` 预缓存清单补全了（之前漏了 timer/audio/segments/
   backup 四个模块），跟这个 bug 关系不大，但万一没信号时 SW 恰好升级，之
   前那个漏洞会让计时器整体加载失败——保险起见提一句，不用专门测。

## 这轮新增

### 1. 备份加固三点（已 push、已上线）
- 未备份提醒条：累计 6 次未备份或距上次 14 天，首页弹「立即备份」
- 「保存备份」按钮走 iOS 分享面板（存备忘录 / 文件 / 发给自己）
- 备份格式加 `version` + `exportedAt`，导入兼容旧格式
- `⚙ 备份` 里有「上次备份：X」状态行

### 2. 跑步计时器（本地已提交，见下方 push）
- 「接下来」卡片上新增 **▶ 开始计时**
- **走跑结合**（月 1 的 12 次）：自动循环 跑 4′→走 1′…，大倒计时 + 「第 2/4 组」+ 总剩时间 + 进度条
- **单段连续跑**（24 次「轻松跑/马拉松心率跑 X 分钟」）：单段倒计时
- **其余 27 次**（节奏跑/间歇/按距离/比赛日）：不自动循环，给一个秒表 + 计划文字
- **提示音**代替震动（iOS 网页不支持震动）：进跑段三声上行、进走段两声下行、段末 3 秒读秒、结束三长音
- **屏幕常亮**（Wake Lock）：开始时申请，暂停/结束释放；回前台自动重申请
- **节拍音**开关：165–190 步/分（默认 180，正好对上教练的步频要求），也顺带帮页面在后台不被挂起
- 结束后可「顺手打卡」
- 页面里写明限制：不支持震动用提示音；锁屏放兜里提示音可能延后

TDD：`src/segments.js`（解析文字→段落）+ `src/timer.js`（时间轴 / stateAt / frameCues 提示音判断 / 时钟格式）+ `src/audio.js`（Web Audio，薄壳）。测试 **78 个用例全绿**。

## 线上地址（已经能用）

## 线上地址（已经能用）

**<https://lihaoran1993912.github.io/run-half-marathon/app/>**

iPhone Safari 打开 → 分享 → 添加到主屏幕。第一次连 WiFi 打开一次（缓存整包），之后断网也能用。

## 代码状态

你在的时候钥匙串是解锁的，我把「备份加固」也顺手 push 了，已上线。
「跑步计时器」这几个提交我也会 push（如果 push 失败就是钥匙串又锁了，你回来 `git push` 一下即可）：

```
cd ~/github/lihaoran1993912/myprojects/run-half-marathon
git status      # 看有没有还没 push 的
git push
```

## 已完成

- **v0.3 主体**：20 周 / 63 次训练（你已核对），按顺序打卡的指针模型，
  进度条 / 「接下来」大卡片 / 六格统计 / 全部计划 / 备份导入导出
- **打卡历史**：新增「最近完成」卡片；「全部计划」里每次训练显示完成日期、每周显示教练的话
- **加固**：完成按钮点一下即禁用（防手抖连点多打卡）；日期输入加无障碍标签；
  文本统一转义；iOS 状态栏样式改成 `default`（避免浅色下状态栏文字看不见）
- 测试 42 个用例全绿（`cd app && npm test`）
- `preview.html` 单文件兜底版已同步重建

## 回来后在手机上过一遍

1. 打卡一次 → 看「最近完成」、进度条、全部计划里的日期
2. 「保存备份」→ 走分享面板存一份到备忘录；看「上次备份」状态行更新
3. **计时器（重点，我没法在这台机器上真机验）**：
   - 第 1 次训练点「▶ 开始计时」→ 点「开始」→ 看倒计时、听跑/走切换提示音、段末读秒
   - 锁屏等 10 秒再解锁 → 倒计时应立刻跳到正确位置（按墙上时间算的）
   - 开「节拍音」听有没有「哒哒」，拖 BPM 有没有变快慢
   - 试一个「轻松跑 30 分钟」的（单段倒计时）和一个节奏跑的（应显示秒表）
   - 结束后点「顺手打卡」
4. 关 WiFi 和蜂窝，从主屏幕图标打开，确认能离线用

## 以后可做（不急）

- 计时器：真机若发现锁屏后提示音延迟太多，考虑加「静音保活音轨」或只做屏幕常亮场景
- 节奏跑/间歇跑：如果你想要，可以让计时器也支持（默认组间休息 90 秒，可调）
- 改错日期：`store.setDate()` 逻辑和测试已有，还没接界面
- 「全部计划」按月折叠；深色模式单独配 `theme-color`
