# 操作系统产生的垃圾文件
.DS_Store
Thumbs.db
desktop.ini

# 编辑器 / IDE
.vscode/
.idea/
*.swp
*~

# Node（app/ 目前零依赖，这行是防以后 npm install 把依赖塞进版本库）
node_modules/

# Office 临时文件
~$*

# 导出的成品（PDF / Word / EPUB 等），放这个文件夹里就不会进版本库
/导出/
