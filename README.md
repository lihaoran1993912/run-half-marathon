# 人人都能跑马拉松

一个帮**零基础跑者**从头训练、最终跑完**半程马拉松（21.0975 公里）**的项目。

## 现在的主体：打卡 App（PWA）

`app/` —— 一个手机网页 App。**5 个月 / 20 周 / 63 次训练**（来自「得到 App」计划表），
逐次打卡。按顺序完成即可，忙了就顺延，把 63 次做完就算达标。

**打开地址**：<https://lihaoran1993912.github.io/run-half-marathon/app/>
（iPhone Safari 打开 → 添加到主屏幕，当 App 用）

- 打卡记录只存手机本机，无账号、无服务器、断网也能用
- 加到 iPhone 主屏幕后当原生 App 用；不引任何外部 CDN，国行 iPhone 也能秒开
- 拆成 `src/` 纯逻辑 + `test/` 测试（Node 内置测试，零依赖），`app.js` 只做渲染
- 发布方式和本地开发见 `app/README.md`

## 目录

| 路径 | 内容 |
|---|---|
| `app/` | ⭐ 打卡 App 的源代码和说明 |
| `训练计划/` | 24 周 / 48 周计划表（文字版参考；App 用的是「得到 App」的 20 周版） |
| `打卡模板/` | 纸质打卡模板（每日 / 每周 / 每月），想手写记录时用 |
| `参考资料/` | 参考书目、术语表 —— App 里训练方法的来源 |
| `正文/`、`大纲.md` | 早期按「写一本书」思路搭的章节框架，现作为背景笔记保留 |
| `更新日志.md` | 项目里程碑 |

## 训练方法一句话

80% 的跑量用「能聊天」的轻松配速打有氧底子；每周加量不超过约 10%，每 4 周减量一次；
长距离最多练到 19 公里；节奏跑和比赛配速训练留到赛前几个月才加重。
方法参考盖洛威走跑结合、丹尼尔斯配速分区、汉森半马专项等，见 `参考资料/参考书目.md`。

## 版本管理

用 Git。每改一块就提交一次，随时能回退。
