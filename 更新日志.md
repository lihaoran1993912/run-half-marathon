# 更新日志

记录项目的关键里程碑。日常小改动看 Git 提交历史即可，这里只记"值得回头看"的节点。

## v0.3 — 2026-09-10

- **按真实计划重做打卡 App**：改用「得到 App」的 5 个月 / 20 周 / 63 次训练计划
  （走跑结合 → 连续轻松跑 → 间歇跑 → 节奏跑 → 马拉松心率跑 + 比赛）
- **打卡模型换成「指针」**：不按日历强制，按顺序完成，忙了顺延，做完总量即达标
- **工程方式**：拆成 `src/` 纯逻辑模块 + `test/` 测试（Node 内置测试，38 用例，零依赖），
  `app.js` 只做渲染；先测试后实现，小步提交
- **PWA**：`manifest` + `sw.js`，加到主屏幕可安装、可离线；不再引 Google 字体等外部 CDN
  （适配国行 iPhone / iOS 26）
- 旧版云端同步（依赖 claude.ai 运行环境）去掉，回归纯本地存储 + 手动备份
- 新增 `serve.js` 本地预览、`build-preview.js` 单文件兜底版
- 上一版单文件 `app/index.html`（40 周 + 云同步）保留在 Git 历史里（提交 609421f）

## v0.2 — 2026-09-09

- **方向调整**：从「写一本书」改为「做一个手机打卡 App」
- 新增 `app/index.html`：单文件网页 App，已发布
  - 40 周训练计划（零基础 → 半马，比赛日 2027-06-24）
  - 每次跑步打卡、倒计时、总进度、云端同步、备份导出
- `README` 改写为以 App 为主体
- `正文/`、`大纲.md` 保留为背景笔记

## v0.1 — 2026-09-09

- 搭好项目框架：目录结构、`README`、`.gitignore`
- 完成总大纲 `大纲.md`：11 个部分 + 36 节 + 附录
- 建好三个打卡模板、两版训练计划表骨架、参考书目、术语表
- 正文各节暂为占位，待逐节撰写
